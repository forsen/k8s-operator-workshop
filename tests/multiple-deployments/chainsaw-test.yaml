apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: normal-working-hours-multiple
spec:
  skip: false
  concurrent: true
  skipDelete: false
  steps:
    - try:
        - create:
            file: definition.yaml
        - assert:
            file: definition-assert.yaml
        - apply:
            file: definition-outside-bh.yaml
        - assert:
            file: definition-assert-outside-bh.yaml
